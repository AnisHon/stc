/**
 * @date {{ generated_time }}
 * Generated, DO NOT EDIT!!!
 */
#pragma once

#include <cstdint>

namespace {{ namespace_name }} {

enum class UnicodeClass: std::uint32_t { {% for entry in tag_entries %}
    {{ entry[0] }} = {{ entry[1] }},
{%- endfor %}
};

struct UnicodeRange {
    const char32_t beg;
    const char32_t end;
    const char32_t tag;
};

/**
 * @brief ICU字符范围数据表
 */
extern const UnicodeRange UNICODE_RANGES[{{ ranges_len }}];

} // namespace {{ namespace_name }}
